{
  "name": "@rtl/ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch",
    "coverage": "vitest run --coverage",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build -o storybook-static --stats-json",
    "lint": "eslint --fix",
    "check-types": "tsc --noEmit",
    "chromatic": "npx chromatic"
  },
  "dependencies": {
    "@rtl/icons": "workspace:*",
    "@rtl/react-primitives": "workspace:*",
    "@rtl/react-utils": "workspace:*"
  },
  "devDependencies": {
    "@rtl/eslint-config": "workspace:*",
    "@rtl/typescript-config": "workspace:*"
  }
}
