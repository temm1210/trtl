{
  "name": "@rtl/ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -b && vite build",
    "lint": "eslint --fix",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@rtl/eslint-config": "workspace:*",
    "@rtl/typescript-config": "workspace:*",
    "@rtl/react-primitives": "workspace:*"
  }
}
